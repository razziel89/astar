// Package astar implements the A* path finding algorithm.
package astar

// FindPath finds the path between the start and end node.
func FindPath(graph []*Node, start *Node, end *Node) ([]*Node, error) {
	return []*Node{}, nil
}
